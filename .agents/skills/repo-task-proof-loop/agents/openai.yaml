interface:
  display_name: "Repo Task Proof Loop"
  short_description: "Init repo-local task artifacts, install role-specific subagents, and drive spec → build → evidence → verify → fix loops."
  default_prompt: "Spawn subagents and use $repo-task-proof-loop to initialize this repository for the repo-local spec -> build -> evidence -> verify -> fix workflow, then derive or find task IDs later from task descriptions as needed."
policy:
  allow_implicit_invocation: false
